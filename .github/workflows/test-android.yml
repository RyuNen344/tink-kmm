name: test-android
on:
  pull_request

jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: sdkman/sdkman-action@v1
        id: sdkman
      - run: java -version
